{
  "functions": {
    "api/upload.js": {
      "runtime": "node@18"
    }
  },
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/$1" }
  ]
}
```

4. Commit changes

---

## Also rename your file

In GitHub, rename `api/upload.js` to confirm it's in the right place — it must be exactly:
```
api/upload.js
```
Not inside any other folder.

---

## After Committing — Test Again

Open in browser:
```
https://premiumcarhub.com.au/api/upload
